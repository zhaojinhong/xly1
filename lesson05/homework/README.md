
```bash
create table 51reboot_users(
   id int unsigned not null primary key auto_increment,
   name VARCHAR(32) NOT NULL,
   age int NOT NULL,
   tel VARCHAR(11) NOT NULL,
   email VARCHAR(30) NOT NULL
);
```